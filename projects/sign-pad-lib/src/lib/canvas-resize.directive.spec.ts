import { CanvasResizeDirective } from './canvas-resize.directive';

describe('CanvasResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasResizeDirective();
    expect(directive).toBeTruthy();
  });
});
